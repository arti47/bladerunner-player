{
  "name": "blade-runner-player",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dev-only regression harness for Blade Runner Player (the app itself has no build step).",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:unit": "node --test tests/unit.test.mjs",
    "test:smoke": "node --test tests/smoke.test.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.44.0"
  }
}
